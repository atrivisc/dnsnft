# domain            [ipv4-set  [ipv6-set]]
#
# 'example.com' matches that name only, '*.example.com' also matches every
# name under it. Without a set, the ones given with -4 and -6 are used; '-'
# leaves that family out.
